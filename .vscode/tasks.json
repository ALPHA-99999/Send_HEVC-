{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build send_lb",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/build",
                "-j2",
                "--target",
                "send_lb"
            ],
            "problemMatcher": []
        },
        {
            "label": "Run send_lb",
            "type": "shell",
            "command": "${workspaceFolder}/build/send_lb",
            "dependsOn": [
                "Build send_lb"
            ],
            "dependsOrder": "sequence",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
